using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TestHP : MonoBehaviour
{
    public Slider UIHP;
    // Start is called before the first frame update
    void Start()
    {
        /*
        int Money = 80;
        PlayerPrefs.SetInt("MyMoney", Money);
        PlayerPrefs.Save();

        int Money_ = PlayerPrefs.GetInt("MyMoney");

        Debug.Log(Money_);
            

        //mSlider = GetComponent<Slider>();
        //mSlider.value = 1f;

        Invoke("Test",2);
        */
    }

    // Update is called once per frame
    void Update()
    {
        //UIHP.maxValue = PlayerStatus.MAXHP;
        //UIHP.value = PlayerStatus.HP;
    }
}
